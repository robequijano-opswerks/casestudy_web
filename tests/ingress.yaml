apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: web-app-ingress
spec:
  rules:
  - host: 143.42.79.220
    http:
      paths:
      - path: /canary
        pathType: Prefix
        backend:
          service:
            name: web-app-service
            port:
              number: 80
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-app-service
            port:
              number: 80
